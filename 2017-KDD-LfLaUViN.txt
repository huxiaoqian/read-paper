20171026
2017-KDD-Learning from Labeled and Unlabeled Vertices in Networks
content:
1 问题:网络中具有有标签的节点和无标签的节点，
如何使用有标签的节点对未标签的节点进行打标签
2 方案:
半监督学习框架---weighted vote Geometric Neighbor Classifier
通过随机游走的方式不止讨论节点局部邻居信息，而是global的节点邻居信息

出发点:
传统的方式是基于一跳邻近节点来推断，
但是当unlabeled节点的邻居节点有很少的labeled的节点时比较困难
所以本文是采用随机游走同时捕获短距离和长距离的网络关系

将长短距离的随机游走情况融合在SVM中







脑洞：
1 都是转化成一个损失函数（不论是以分类体系的logistic-sigmod函数还是矩阵范式的作为惩罚项）进行优化，节点表示方法，通常是副产品。
2 脑洞？link2vec---以增量学习的方式,新的边加入后对原有边的表示更新以及新的边的表示？？
具有相同顶点的边相近
具有边---边的相似度度量？
3 脑洞？网络中有些边是缺乏，而有些边是错误连接的，
但是目前很多方法都是将没有出现的认为是相似度0
但是连接上的认为具有一定相似度，如何规避这个问题？？---推荐问题中应该会出现？
